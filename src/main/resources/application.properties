# datasource
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.url=jdbc:sqlite:database.sqlite
mybatis-plus.config-location=classpath:configs/mybatis.xml

# server
server.servlet.context-path=/media